"""Google integration (Calendar + Meet) — phase 3.

Kept thin but importable: all Google API SDKs are imported lazily inside the
functions/methods that use them, so this package imports cleanly even when the
Google client libraries are not installed.
"""
